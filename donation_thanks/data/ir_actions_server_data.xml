<?xml version="1.0" encoding="utf-8" ?>
<odoo noupdate="1">
    <record id="action_donation_donation_email" model="ir.actions.server">
        <field name="name">Email Donation Thanks DIRECTLY</field>
        <field name="model_id" ref="donation.model_donation_donation"/>
        <field name="binding_model_id" ref="donation.model_donation_donation"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
mail_template = env.ref("donation_thanks.mail_template_donation_donation")

for record in records:
    mail_template.send_mail(record.id)

action = {
    'type': 'ir.actions.act_window',
    "name": "Send with Email Queue Manager (Scheduled Action)",
    "views": [[False, "tree"], [False, "form"]],
    'res_model': 'mail.mail',
}
        ]]></field>
    </record>
    <record id="action_donation_tax_receipt_email" model="ir.actions.server">
        <field name="name">Email Tax Receipt Thanks DIRECTLY</field>
        <field name="model_id" ref="donation_base.model_donation_tax_receipt"/>
        <field name="binding_model_id" ref="donation_base.model_donation_tax_receipt"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
mail_template = env.ref("donation_thanks.mail_template_donation_tax_receipt")

for record in records:
    mail_template.send_mail(record.id)

action = {
    'type': 'ir.actions.act_window',
    "name": "Send with Email Queue Manager (Scheduled Action)",
    "views": [[False, "tree"], [False, "form"]],
    'res_model': 'mail.mail',
}
        ]]></field>
    </record>
</odoo>
