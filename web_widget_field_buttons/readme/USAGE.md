Use the `popover` widget on `Char` or `Text` fields.

```xml
<field
    name="selection_field"
    widget="selection_buttons"
/>
```

```xml
<field
    name="many2one_field"
    widget="many2one_buttons"
/>
```

```xml
<field
    name="many2many_field"
    widget="many2many_buttons"
/>
```
