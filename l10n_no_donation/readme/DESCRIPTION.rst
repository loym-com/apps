This module adds a wizard to export a donation XML report for the Norwegian tax office.

FIXME

"Organisasjonsnummer" should be without the NO prefix, but it is currently exported with the prefix.

"Kontaktinformasjon" should be added to the XML, but it is currently missing.

PDF: Name = report.[report_name].pdf, but it should be just [report_name].pdf (not beginning with "report.").
